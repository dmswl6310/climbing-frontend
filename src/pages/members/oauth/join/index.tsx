import styled from "styled-components";

const AdditionalJoin = (data: any) => {
  console.log(data);
  return <S.Wrapper>리다이렉트 페이지</S.Wrapper>;
};

const S = {
  Wrapper: styled.div``,
};

export default AdditionalJoin;
